# Section photos are content-hashed by filename only when their content
# changes (we rename, e.g. automation-blue -> automation-blue2), so repeat
# visits can treat them as immutable for a year.
/automation-*
  Cache-Control: public, max-age=31536000, immutable
